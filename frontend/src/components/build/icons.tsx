/**
 * Icons for the builder (VLP) interface, from Lucide. Kept behind local aliases
 * so panels import a stable name regardless of the underlying icon set.
 */
export {
  Mic as MicIcon,
  Pencil as PencilIcon,
  CircleHelp as QuestionIcon,
} from "lucide-react";
